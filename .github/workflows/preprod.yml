name: Build & Release (preprod)

on:
  push:
    tags: ['preprod-*']
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ !startsWith(github.ref, 'refs/tags/') }}

# ── Fill in your non-secret app env vars here ─────────────────────────────────
env:
  S3_BUCKET: vaarta-app
  BASE_URL: https://vaarta-app.ekacare.co
  NEXT_PUBLIC_ENV: PROD
  ELECTRON_ENV: |
    API_URL=https://api.eka.care
    SENTRY_DSN=
    NEXT_PUBLIC_ENV=PROD


jobs:
  # ─── Windows ──────────────────────────────────────────────────────────────
  build-windows:
    runs-on: windows-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: |
            package-lock.json
            external/ekascribe/package-lock.json

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Install AzureSignTool
        run: dotnet tool install --global AzureSignTool

      - run: npm ci

      - name: Assume IAM role for secrets
        uses: aws-actions/configure-aws-credentials@e3dd6a429d7300a6a4c196c26e071d42e0343502
        with:
          role-to-assume: arn:aws:iam::559615561845:role/github-secrets-role
          role-session-name: secret-role
          aws-region: ${{ secrets.PROD_AWS_Region }}

      - name: Read secrets from AWS Secrets Manager
        uses: abhilash1in/aws-secrets-manager-action@v2.1.0
        with:
          secrets: |
            prod/scribe-web/all
          parse-json: false

      - name: Export secrets as build env vars
        shell: bash
        run: |
          echo "$PROD_SCRIBE_WEB_ALL" | jq -r 'to_entries[] | "\(.key)=\(.value)"' >> $GITHUB_ENV

      - name: Write electron.env
        shell: bash
        run: |
          echo "$ELECTRON_ENV" > electron.env
          echo "NEXT_PUBLIC_FIREBASE_API_KEY=$NEXT_PUBLIC_FIREBASE_API_KEY" >> electron.env
          echo "NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=$NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN" >> electron.env
          echo "NEXT_PUBLIC_FIREBASE_PROJECT_ID=$NEXT_PUBLIC_FIREBASE_PROJECT_ID" >> electron.env
          echo "NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=$NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET" >> electron.env
          echo "NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=$NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID" >> electron.env
          echo "NEXT_PUBLIC_FIREBASE_APP_ID=$NEXT_PUBLIC_FIREBASE_APP_ID" >> electron.env

      - name: Package
        run: npm run package

      - name: Build Windows helpers (x64 + arm64)
        run: npm run build:win-helper:all

      - name: Build NSIS installers
        shell: pwsh
        run: |
          $env:Path = "$env:USERPROFILE\.dotnet\tools;$env:Path"
          npx electron-builder --win nsis --x64 --arm64 --publish never
        env:
          AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          AZURE_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-dist
          path: |
            dist/*.exe
            dist/*.exe.blockmap
            dist/latest.yml
          retention-days: 1

  # ─── macOS ────────────────────────────────────────────────────────────────
  build-mac:
    runs-on: macos-26 # arm64 (M-series)
    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: |
            package-lock.json
            external/ekascribe/package-lock.json

      - run: npm ci

      - name: Assume IAM role for secrets
        uses: aws-actions/configure-aws-credentials@e3dd6a429d7300a6a4c196c26e071d42e0343502
        with:
          role-to-assume: arn:aws:iam::559615561845:role/github-secrets-role
          role-session-name: secret-role
          aws-region: ${{ secrets.PROD_AWS_Region }}

      - name: Read secrets from AWS Secrets Manager
        uses: abhilash1in/aws-secrets-manager-action@v2.1.0
        with:
          secrets: |
            prod/scribe-web/all
          parse-json: false

      - name: Export secrets as build env vars
        run: |
          echo "$PROD_SCRIBE_WEB_ALL" | jq -r 'to_entries[] | "\(.key)=\(.value)"' >> $GITHUB_ENV

      - name: Write electron.env
        run: |
          echo "$ELECTRON_ENV" > electron.env
          echo "NEXT_PUBLIC_FIREBASE_API_KEY=$NEXT_PUBLIC_FIREBASE_API_KEY" >> electron.env
          echo "NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=$NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN" >> electron.env
          echo "NEXT_PUBLIC_FIREBASE_PROJECT_ID=$NEXT_PUBLIC_FIREBASE_PROJECT_ID" >> electron.env
          echo "NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=$NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET" >> electron.env
          echo "NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=$NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID" >> electron.env
          echo "NEXT_PUBLIC_FIREBASE_APP_ID=$NEXT_PUBLIC_FIREBASE_APP_ID" >> electron.env

      - name: Build macOS helper
        run: |
          xcodebuild \
            -project mac/EkaCareDesktopHelper/EkaCareDesktopHelper.xcodeproj \
            -scheme EkaCareDesktopHelper \
            -configuration Release \
            build -quiet \
            SYMROOT="$PWD/mac/build" \
            CONFIGURATION_BUILD_DIR="$PWD/mac/build/Release" \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO

      - name: Package
        run: npm run package

      - name: Build macOS installers
        run: npx electron-builder --mac dmg zip --publish never
        env:
          CSC_LINK: ${{ secrets.CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ secrets.CSC_KEY_PASSWORD }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}

      - name: Upload macOS artifacts
        uses: actions/upload-artifact@v4
        with:
          name: mac-dist
          path: |
            dist/*.dmg
            dist/*.dmg.blockmap
            dist/*.zip
            dist/latest-mac.yml
          retention-days: 1

  # ─── Deploy to S3 ────────────────────────────────────────────────────────
  deploy:
    needs: [build-windows, build-mac]
    runs-on: [dev-ec2-server-2]
    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/download-artifact@v4
        with:
          name: windows-dist
          path: dist/

      - uses: actions/download-artifact@v4
        with:
          name: mac-dist
          path: dist/

      - name: Sync to S3
        run: |
          VERSION=${GITHUB_REF_NAME#preprod-}

          # Versioned archive — organized by platform/arch
          aws s3 sync dist/ s3://$S3_BUCKET/dev/${VERSION}/windows/x64/ \
            --exclude "*" \
            --include "*.exe" --include "*.exe.blockmap" \
            --exclude "*arm64*"

          aws s3 sync dist/ s3://$S3_BUCKET/dev/${VERSION}/windows/arm64/ \
            --exclude "*" \
            --include "*arm64*.exe" --include "*arm64*.exe.blockmap"

          aws s3 sync dist/ s3://$S3_BUCKET/dev/${VERSION}/mac/arm64/ \
            --exclude "*" \
            --include "*.dmg" --include "*.dmg.blockmap" \
            --include "*.zip" --include "*.zip.blockmap"

          aws s3 cp dist/latest.yml     s3://$S3_BUCKET/dev/${VERSION}/
          aws s3 cp dist/latest-mac.yml s3://$S3_BUCKET/dev/${VERSION}/

          # latest/ stays flat — auto-updater resolves binary paths relative to latest.yml
          aws s3 sync dist/ s3://$S3_BUCKET/dev/latest/ --delete

      - name: Print download URLs
        run: |
          VERSION=${GITHUB_REF_NAME#preprod-}
          BASE=$BASE_URL/dev
          {
            echo "## Vaarta ${VERSION} (preprod) — Download URLs"
            echo ""
            echo "### Versioned \`${VERSION}\`"
            echo "| Platform | Arch | Link |"
            echo "| --- | --- | --- |"
            echo "| Windows | x64 | [Vaarta Setup.exe](${BASE}/${VERSION}/windows/x64/Vaarta%20Setup.exe) |"
            echo "| Windows | arm64 | [Vaarta Setup-arm64.exe](${BASE}/${VERSION}/windows/arm64/Vaarta%20Setup-arm64.exe) |"
            echo "| macOS | arm64 | [Vaarta.dmg](${BASE}/${VERSION}/mac/arm64/Vaarta.dmg) |"
            echo "| macOS | arm64 (zip) | [Vaarta.zip](${BASE}/${VERSION}/mac/arm64/Vaarta.zip) |"
            echo ""
            echo "### Latest"
            echo "| Platform | Arch | Link |"
            echo "| --- | --- | --- |"
            echo "| Windows | x64 | [Vaarta Setup.exe](${BASE}/latest/Vaarta%20Setup.exe) |"
            echo "| Windows | arm64 | [Vaarta Setup-arm64.exe](${BASE}/latest/Vaarta%20Setup-arm64.exe) |"
            echo "| macOS | arm64 | [Vaarta.dmg](${BASE}/latest/Vaarta.dmg) |"
            echo "| macOS | arm64 (zip) | [Vaarta.zip](${BASE}/latest/Vaarta.zip) |"
          } >> "$GITHUB_STEP_SUMMARY"
